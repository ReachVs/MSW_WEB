import './index.css'

function App() {
  return (
    <main className="app-shell">
      <section className="starter-card">
        <p className="eyebrow">MSW Client Side</p>
        <h1>React customer UI starter</h1>
        <p>
          This frontend is intentionally fresh. Build the public customer
          experience here while the Laravel Blade admin stays separate.
        </p>
      </section>
    </main>
  )
}

export default App
